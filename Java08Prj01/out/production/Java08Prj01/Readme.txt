Function functional interface:
==============================

    1. Going to use java.util.function.Function interface on a list of Integers in order to return a list
        of Double using lambda expression
    2. Use generic to generalize the above code
    3. Will show the usage of the andThen, compose default methods.